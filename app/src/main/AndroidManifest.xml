<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ck5000.thuan.nongdanbiet">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BeginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".Begin1Activity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".GiaCaActivity"/>
        <activity android:name=".PostActivity"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"/>
        <activity android:name=".Post1Activity"/>
        <activity android:name=".GroupActivity"/>
        <activity android:name=".BaiBaoActivity"/>
        <activity android:name=".NewActivity"/>
        <activity
            android:name=".weatherprediction.WeatherForecast"
            android:label="Dự báo thời tiết các ngày tiếp theo">
        </activity>
        <activity
            android:name=".weatherprediction.WeatherFore"
            android:label="Dự báo thời tiết các ngày tiếp theo">
        </activity>
        <activity
            android:name=".weatherprediction.WeatherCurrentLocationActivity"
            android:label="@string/title_activity_weather_current_location">
        </activity>
        <activity
            android:name=".weatherprediction.WeatherByAddressActivity"
            android:label="@string/title_activity_weather_by_address">
        </activity>
        <activity
            android:name=".weatherprediction.ChooseAddressActivity"
            android:label="@string/title_activity_choose_address">
        </activity>
        <activity
            android:name=".ThoiTietActivity"
            android:label="@string/thoi_tiet"/>
        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2">
        </activity>
    </application>

</manifest>